{
  "_comment": "Ollama on the HOST, reached from inside the container (no API key). Pull a model on the host first: `ollama pull llama3.2` — and override the model with -e ORKEON_Llm__Model=<name> if you pulled another one. Linux engine (no Docker Desktop): add --add-host=host.docker.internal:host-gateway to `docker run`.",
  "Llm": {
    "Model": "llama3.2",
    "BaseUrl": "http://host.docker.internal:11434",
    "ApiKey": "not-needed",
    "Temperature": 0.7,
    "MaxTokens": 4096,
    "TimeoutSeconds": 120
  },
  "RateLimiting": {
    "MaxConcurrentRequests": 1,
    "GlobalRequestsPerMinute": 60,
    "ProviderRequestsPerMinute": 30,
    "AgentRequestsPerMinute": 20,
    "QueueLimit": 32
  }
}
