using System.Windows.Controls;

namespace Orkeon.Studio.Wpf.Views;

/// <summary>The "Limites &amp; journaux" settings tab. XAML wiring only; every behaviour lives in the ViewModel.</summary>
public partial class LimitsView : UserControl
{
    /// <summary>Loads the XAML.</summary>
    public LimitsView() => InitializeComponent();
}
