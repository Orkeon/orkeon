using System.Windows.Controls;

namespace Orkeon.Studio.Wpf.Views;

/// <summary>The "TestTeamView" screen. XAML wiring only; every behaviour lives in the ViewModel.</summary>
public partial class TestTeamView : UserControl
{
    /// <summary>Loads the XAML.</summary>
    public TestTeamView() => InitializeComponent();
}
