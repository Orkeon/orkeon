using System.Windows.Controls;

namespace Orkeon.Studio.Wpf.Views;

/// <summary>The Studio settings tab (STUDIO-35). XAML wiring only; every behaviour lives in the ViewModels.</summary>
public partial class StudioSettingsView : UserControl
{
    /// <summary>Loads the XAML.</summary>
    public StudioSettingsView() => InitializeComponent();
}
