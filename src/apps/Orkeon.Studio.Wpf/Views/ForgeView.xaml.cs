using System.Windows.Controls;

namespace Orkeon.Studio.Wpf.Views;

/// <summary>The "Résoudre" screen. XAML wiring only; every behaviour lives in the ViewModel.</summary>
public partial class ForgeView : UserControl
{
    /// <summary>Loads the XAML.</summary>
    public ForgeView() => InitializeComponent();
}
