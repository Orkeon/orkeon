using System.Windows.Controls;

namespace Orkeon.Studio.Wpf.Views;

/// <summary>The "ImportTeamView" screen. XAML wiring only; every behaviour lives in the ViewModel.</summary>
public partial class ImportTeamView : UserControl
{
    /// <summary>Loads the XAML.</summary>
    public ImportTeamView() => InitializeComponent();
}
