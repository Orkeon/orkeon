using System.Windows.Controls;

namespace Orkeon.Studio.Wpf.Views;

/// <summary>"Mes solutions". XAML wiring only; every behaviour lives in the ViewModel.</summary>
public partial class ForgeSolutionsView : UserControl
{
    /// <summary>Loads the XAML.</summary>
    public ForgeSolutionsView() => InitializeComponent();
}
