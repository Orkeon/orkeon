using System.Windows.Controls;

namespace Orkeon.Studio.Wpf.Views;

/// <summary>The "Sections" screen. XAML wiring only; every behaviour lives in the ViewModel.</summary>
public partial class SectionsView : UserControl
{
    /// <summary>Loads the XAML.</summary>
    public SectionsView() => InitializeComponent();
}
