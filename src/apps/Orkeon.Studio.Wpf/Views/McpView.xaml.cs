using System.Windows.Controls;

namespace Orkeon.Studio.Wpf.Views;

/// <summary>The MCP settings tab (STUDIO-21). XAML wiring only; every behaviour lives in the ViewModels.</summary>
public partial class McpView : UserControl
{
    /// <summary>Loads the XAML.</summary>
    public McpView() => InitializeComponent();
}
