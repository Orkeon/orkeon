using System.Windows.Controls;

namespace Orkeon.Studio.Wpf.Views;

/// <summary>The tools settings tab (STUDIO-21). XAML wiring only; every behaviour lives in the ViewModels.</summary>
public partial class ToolsView : UserControl
{
    /// <summary>Loads the XAML.</summary>
    public ToolsView() => InitializeComponent();
}
