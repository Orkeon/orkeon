# _shared - Shared Resources

Resources shared across multiple examples.

## Contents

| Directory | Purpose |
|-----------|---------|
| `tools/` | Custom tool implementations reusable across examples |
| `templates/` | YAML configuration templates for common patterns |

## Usage

Place shared tools in the `tools/` directory and reference them from example configurations. Templates in `templates/` provide starting points for creating new example configurations.
